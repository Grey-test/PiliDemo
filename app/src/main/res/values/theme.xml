<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App base theme-->
    <style name="Base.AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="actionModeBackground">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/colorControlHighlight</item>
        <item name="alertDialogTheme">@style/Theme.AppCompat.DayNight.Dialog.Alert</item>
    </style>

    <!-- App theme-->
    <style name="AppTheme" parent="Base.AppTheme" />

    <!-- Translucent status bar theme-->
    <style name="FullScreenTheme" parent="Base.AppTheme" />

    <!-- Now Playing theme-->
    <style name="NowPlayingTheme" parent="FullScreenTheme">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- App Home theme-->
    <style name="HomeTheme" parent="Base.AppTheme" />

    <!-- Translucent theme for LaunchActivity-->
    <style name="TranslucentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="alertDialogTheme">@style/Theme.AppCompat.Light.Dialog.Alert</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoDisplay">true</item>
    </style>

</resources>